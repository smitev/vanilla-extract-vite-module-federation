import * as styles from './styles.css'

export const RemoteApp = () => {

  return (
    <section className={styles.remoteApp}>
      <h2>Remote app</h2>
    </section>
  );
};

export default RemoteApp;
